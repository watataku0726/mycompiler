// hogehoge
hoge
#define HOGE(i, defined, k) i + defined + k 

#define HOGEE (i, j, k) i + j + k 
#if defined(HOGE) HOGEE
int u;
#endif
//#undef defined HOGE HOGEE
//#undef
HOGEE
u = HOGE(3, 4 ,5);
int main(int argc, char** argv) {
    char hoge = '\a';
    char* fuga = "hogehoge\"\0\n";
    float bar = 3.4e-3f;
    float boo = 3.4E-3;
    int __ = 32u + 43ll * 1003UL / boo && 9l + 0x10;
    unsigned long long kore = '!' 
     + '"' + '#' + '$' + '%' + '&' + '(' + ')' + '*' + '+' + ',' + '-'  + '.' + '/' + ':' + ';' + '<' + '=' + '>' + '\'' + '?' + '@' + '[' + '\\' + ']' + '^' + '_' + '`' + '{' + '|' + '}' + '~';
    return 0;
}

typedef int INT;
INT i = 0;

/*
#define i i##KUG + FUIG / 9 ## ## f
#define PM #pm
#define HOGE(i, ij, k, l, m)  ` k i ##  ##  m l  ij ##  #k
#define DEFINE //#define 
#define defined
#ifndef m
int ya = 0;
#endif
#if defined HOG
DEFINE HOG int
#define FUGA(fuga, io)

HOG
//#define HOGE i ; 
//#undef HOG
#elif 1 32
int i;
int define = 0x0UL 013;
int INT = HOGE(P,45UL,3,4,M )

32UL 64ul

#endif*/

#if 0x1ULL + ~0UL
int i32 = sizeof(int);
#endif
